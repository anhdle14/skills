# Deno cache (deps are locked via deno.lock, cache is local)
.deno/

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.python-version
.venv/
venv/
env/
ENV/
build/
dist/
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
.pi/

# Local Claude Code settings (machine-specific permissions)
.claude/settings.local.json

# ship workflow artifacts (plans, specs, slice state, reviews) — never committed
.ship/

# Virtual skill-eval harness (fixtures, runs, judgments) — never committed
.skill-eval/
